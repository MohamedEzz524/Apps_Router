import logo from '../../assets/GradLogo.png';

const Logo = () => {
  return (
    <img
      src={logo}
      alt="LogoIcon"
      className="shadow-shadowGlow size-10 rounded-full border-2"
    />
  );
};

export default Logo;
